[build]
  command = "pnpm install --frozen-lockfile && pnpm exec expo export -p web"
  publish = "dist"

# Single Page Application (SPA) کے لیے ضروری: تاکہ ریفریش کرنے پر 404 ایرر نہ آئے
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200